import {
  HabitatValues,
  RarityValues,
  RegionValues,
  SpeciesTypeValues,
} from "~/lib/types";

export const MAX_BALANCE = 1000000000;
export const MAX_YIELD = 1000000000;

export const WILDCARD_COST = 10;
export const SHINY_WILDCARD_COST = 100;

export const RegionsList = Object.values(RegionValues);
export const RaritiesList = Object.values(RarityValues);
export const TypesList = Object.values(SpeciesTypeValues);
export const HabitatList = Object.values(HabitatValues);

export const CATCHING_CHARM_ID = 1;
